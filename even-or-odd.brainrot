#include <stdio.h>
rizz main() {
    rizz num;
    yapping("Enter an integer: ");
    scanf("%d", &num);

    // true if num is perfectly divisible by 2
    edgy(num % 2 == 0)
        yapping("%d is even.", num);
    amogus
        yapping("%d is odd.", num);
    
    bussin 0;
}
